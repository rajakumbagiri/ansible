- name: install and run ngnix
  hosts: web
  become: yes
  tasks:
   - name: install ngnix
     ansible.builtin.dnf:
       name: nginix
       state: latest
   - name: start ngnix
     ansible.builtin.service:
       name: ngnix
       state: started
       enable: yes